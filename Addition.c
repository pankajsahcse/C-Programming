#include <stdio.h>

int main() {
  int a;
  int b;
  printf("enter the 1st number :");
  scanf("%d", &a);
  printf("enter the 2nd number :");
  scanf("%d", &b);
 printf("Sum of the number which  have you Enter is given below :"); 
 printf("%d", a+b);
   return 0;
}
